tools/source_*.json
tools/raw_*.json
# Built translation DBs are hosted as GitHub Release assets, never committed
# (the bundled bible-reader/bible.sqlite is the only in-repo DB).
tools/*.sqlite

# Xcode
*.xcuserstate
xcuserdata/
.DS_Store
*.xcresult
# Stray compiled scratch binary (build-script artifact, never a source file)
/main

# Python
__pycache__/
*.pyc

# Icon build output
tools/icon/out/

docs/superpowers
.idea
